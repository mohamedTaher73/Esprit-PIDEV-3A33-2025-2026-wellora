{% extends 'layouts/app.html.twig' %}

{% block title %}AI Prediction - Wellora{% endblock %}

{% block body %}
    <!-- Main Content Area with Sidebar -->
    <div class="flex-1 ml-64 min-h-screen transition-all duration-300" :class="collapsed ? 'ml-20' : 'ml-64'">
    <!-- Page Header -->
    <div class="bg-white shadow-sm dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
            <div class="flex items-center justify-between">
                <div>
                    <h1 class="text-2xl font-bold text-gray-900 dark:text-white flex items-center gap-3">
                        <span class="p-2 bg-gradient-to-br from-purple-500 to-indigo-600 rounded-lg">
                            <svg class="w-6 h-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
                            </svg>
                        </span>
                        AI Blood Sugar Prediction
                    </h1>
                    <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">
                        Predictive analysis based on your health data using Machine Learning
                    </p>
                </div>
                <a href="{{ path('app_health_journal_accessible') }}" class="inline-flex items-center px-4 py-2 bg-indigo-600 hover:bg-indigo-700 text-white text-sm font-medium rounded-lg transition-colors shadow-sm">
                    <svg class="w-4 h-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                    </svg>
                    New Entry
                </a>
            </div>
        </div>
    </div>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
            <!-- Main Prediction Card -->
            <div class="lg:col-span-2">
                <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 overflow-hidden">
                    <!-- Card Header -->
                    <div class="bg-gradient-to-r from-purple-600 to-indigo-600 px-6 py-4">
                        <h2 class="text-lg font-semibold text-white flex items-center gap-2">
                            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
                            </svg>
                            Prediction Result
                        </h2>
                    </div>

                    <div class="p-6">
                        {% if prediction %}
                            {% if prediction.isSuccessful() %}
                                <!-- Success State -->
                                <div class="text-center py-8">
                                    <!-- Prediction Value -->
                                    <div class="relative inline-flex">
                                        <div class="w-40 h-40 rounded-full bg-gradient-to-br from-purple-100 to-indigo-100 dark:from-purple-900/30 dark:to-indigo-900/30 flex items-center justify-center border-4 border-purple-200 dark:border-purple-700">
                                            <div class="text-center">
                                                <span class="text-4xl font-bold text-purple-600 dark:text-purple-400">
                                                    {{ prediction.predictedValue|number_format(2, '.', ',') }}
                                                </span>
                                                <span class="text-sm text-purple-500 dark:text-purple-400 block">g/L</span>
                                            </div>
                                        </div>
                                        <div class="absolute -top-2 -right-2">
                                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-bold
                                                {% if prediction.confidenceLevel == 'high' %}
                                                    bg-green-100 text-green-800 dark:bg-green-800 dark:text-green-200
                                                {% elseif prediction.confidenceLevel == 'medium' %}
                                                    bg-yellow-100 text-yellow-800 dark:bg-yellow-800 dark:text-yellow-200
                                                {% else %}
                                                    bg-gray-100 text-gray-800 dark:bg-gray-700 dark:text-gray-300
                                                {% endif %}
                                            ">
                                                {{ prediction.confidenceLevel|upper }}
                                            </span>
                                        </div>
                                    </div>

                                    <!-- Info Grid -->
                                    <div class="grid grid-cols-2 gap-4 mt-8 max-w-md mx-auto">
                                        <div class="bg-gray-50 dark:bg-gray-700/50 rounded-lg p-4">
                                            <p class="text-xs text-gray-500 dark:text-gray-400 uppercase tracking-wide">Entries used</p>
                                            <p class="text-xl font-bold text-gray-900 dark:text-white mt-1">{{ prediction.entriesUsed }}</p>
                                        </div>
                                        <div class="bg-gray-50 dark:bg-gray-700/50 rounded-lg p-4">
                                            <p class="text-xs text-gray-500 dark:text-gray-400 uppercase tracking-wide">Confidence</p>
                                            <p class="text-xl font-bold text-gray-900 dark:text-white mt-1">
                                                {% if prediction.confidenceLevel == 'high' %}High{% elseif prediction.confidenceLevel == 'medium' %}Medium{% else %}Low{% endif %}
                                            </p>
                                        </div>
                                    </div>
                                </div>

                            {% else %}
                                <!-- Failure State -->
                                <div class="bg-amber-50 dark:bg-amber-900/20 border border-amber-200 dark:border-amber-700 rounded-lg p-6">
                                    <div class="flex items-start gap-4">
                                        <div class="flex-shrink-0">
                                            <svg class="w-6 h-6 text-amber-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                                            </svg>
                                        </div>
                                        <div>
                                            <h3 class="text-lg font-medium text-amber-900 dark:text-amber-200">
                                                Insufficient data
                                            </h3>
                                            <p class="mt-2 text-amber-700 dark:text-amber-300">
                                                {{ prediction.message }}
                                            </p>
                                        </div>
                                    </div>
                                </div>
                            {% endif %}
                        {% else %}
                            <!-- No Journal State -->
                            <div class="bg-gray-50 dark:bg-gray-700/50 border border-gray-200 dark:border-gray-600 rounded-lg p-8 text-center">
                                <svg class="w-12 h-12 text-gray-400 mx-auto mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                                </svg>
                                <h3 class="text-lg font-medium text-gray-900 dark:text-white mb-2">
                                    No health journal found
                                </h3>
                                <p class="text-gray-500 dark:text-gray-400 mb-4">
                                    Create a health journal first to start tracking.
                                </p>
                                <a href="{{ path('app_health_journal_accessible') }}" class="inline-flex items-center px-4 py-2 bg-indigo-600 hover:bg-indigo-700 text-white text-sm font-medium rounded-lg transition-colors">
                                    Create entry
                                </a>
                            </div>
                        {% endif %}
                    </div>
                </div>

                <!-- How It Works Card -->
                <div class="mt-6 bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 p-6">
                    <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4 flex items-center gap-2">
                        <svg class="w-5 h-5 text-indigo-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        How does this prediction work?
                    </h3>
                    <div class="space-y-4">
                        <div class="flex gap-4">
                            <div class="flex-shrink-0 w-8 h-8 rounded-full bg-indigo-100 dark:bg-indigo-900/30 flex items-center justify-center">
                                <span class="text-sm font-bold text-indigo-600 dark:text-indigo-400">1</span>
                            </div>
                            <div>
                                <p class="font-medium text-gray-900 dark:text-white">Data collection</p>
                                <p class="text-sm text-gray-500 dark:text-gray-400">The model uses your historical data (sleep, blood pressure, weight)</p>
                            </div>
                        </div>
                        <div class="flex gap-4">
                            <div class="flex-shrink-0 w-8 h-8 rounded-full bg-indigo-100 dark:bg-indigo-900/30 flex items-center justify-center">
                                <span class="text-sm font-bold text-indigo-600 dark:text-indigo-400">2</span>
                            </div>
                            <div>
                                <p class="font-medium text-gray-900 dark:text-white">ML training</p>
                                <p class="text-sm text-gray-500 dark:text-gray-400">A linear regression model is trained on your data</p>
                            </div>
                        </div>
                        <div class="flex gap-4">
                            <div class="flex-shrink-0 w-8 h-8 rounded-full bg-indigo-100 dark:bg-indigo-900/30 flex items-center justify-center">
                                <span class="text-sm font-bold text-indigo-600 dark:text-indigo-400">3</span>
                            </div>
                            <div>
                                <p class="font-medium text-gray-900 dark:text-white">Prediction</p>
                                <p class="text-sm text-gray-500 dark:text-gray-400">Future blood sugar is predicted based on the last 7 days</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Sidebar -->
            <div class="space-y-6">
                <!-- Tips Card -->
                <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 p-6">
                    <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4 flex items-center gap-2">
                        <svg class="w-5 h-5 text-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
                        </svg>
                        Tips
                    </h3>
                    <ul class="space-y-3">
                        <li class="flex items-start gap-3 text-sm text-gray-600 dark:text-gray-300">
                            <svg class="w-5 h-5 text-green-500 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                            </svg>
                            <span>Record your data daily</span>
                        </li>
                        <li class="flex items-start gap-3 text-sm text-gray-600 dark:text-gray-300">
                            <svg class="w-5 h-5 text-green-500 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                            </svg>
                            <span>More data = more accurate prediction</span>
                        </li>
                        <li class="flex items-start gap-3 text-sm text-gray-600 dark:text-gray-300">
                            <svg class="w-5 h-5 text-green-500 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                            </svg>
                            <span>Include sleep and blood pressure</span>
                        </li>
                    </ul>
                </div>

                <!-- Navigation -->
                <div class="flex flex-col gap-3">
                    <a href="{{ path('app_health_journal_accessible') }}" class="w-full px-4 py-3 bg-indigo-600 hover:bg-indigo-700 text-white text-center font-medium rounded-lg transition-colors flex items-center justify-center gap-2">
                        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                        </svg>
                        Add entry
                    </a>
                    <a href="{{ path('app_health_dashboard') }}" class="w-full px-4 py-3 bg-gray-100 dark:bg-gray-700 hover:bg-gray-200 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-300 text-center font-medium rounded-lg transition-colors flex items-center justify-center gap-2">
                        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
                        </svg>
                        Back to dashboard
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}